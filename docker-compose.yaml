services:
  ocs-ad-inventory:
    image: registry.satnusa.com/ocs-ad-inventory-management:latest
    container_name: ocs-ad-inventory
    env_file:
      - .env
    networks:
      - ocs-itop-ad_network

networks:
    ocs-itop-ad_network:
      external: true
      # driver: bridge